
<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>URL Shortener</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bulma@0.9.0/css/bulma.min.css">
  </head>
  <body>
  <section class="section">
    <div class="container">
        <% baseUrl = "http://localhost:5000/" %>
        <table class="table">
            <thead>
                <tr>
                    <th>Original URL</th>
                    <th>Short URL</th>
                    <th>Visits</th>
                </tr>
            </thead>
            <tfoot>
              
            </tfoot>
            <tbody>
              <% for (link of links) { %>
              <tr>
                <td><%= link.originalUrl %></td>
                <td>"<%= baseUrl +  link.shortUrl %>"</td>
                <td><%= link.visits %></td>
            </tr>
            <% } %>
            </tbody>
          </table>
    </div>
  </section>
  </body>
</html>